name: Deploy website
on:
  workflow_dispatch:
    inputs:
      SITE_ID:
        description: "Netlify Site ID"
        required: true
        type: string
      CLIENT_ID:
        description: "Client ID"
        required: true
        type: string

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Checkout
        uses: actions/checkout@v4
      - name: Setup NodeJS
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"
      - name: Install Netlify
        run: npm install netlify-cli@17.10.1 -g --legacy-peer-deps
      - name: Install Next.js Netlify Plugin
        run: npm install -D @netlify/plugin-nextjs --legacy-peer-deps
      - name: Deploy to Netlify
        id: netlify_deploy
        run: |
          netlify deploy \
            --auth ${{ secrets.NETLIFY_AUTH_TOKEN }} --prod --debug
      - name: Netlify Site Info
        run: |
          netlify sites:info ${{ inputs.SITE_ID }} --auth ${{ secrets.NETLIFY_AUTH_TOKEN }}
